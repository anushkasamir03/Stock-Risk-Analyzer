[1mdiff --git a/main.py b/main.py[m
[1mindex c70cab9..8016ff6 100644[m
[1m--- a/main.py[m
[1m+++ b/main.py[m
[36m@@ -2,38 +2,179 @@[m [mimport yfinance as yf[m
 import pandas as pd[m
 import numpy as np[m
 import matplotlib.pyplot as plt[m
[32m+[m[32mimport anthropic[m
 [m
[31m-print("\n=== Stock Risk & Return Analyzer ===\n")[m
[32m+[m[32mANTHROPIC_KEY = "your-api-key-here"  # Replace with your actual key[m
 [m
[31m-ticker = input("Enter stock ticker (example: AAPL): ").upper()[m
[32m+[m[32mprint("************* Smart Money Stock Analyser *************")[m
 [m
[31m-print("\nDownloading data...\n")[m
[31m-data = yf.download(ticker, period="5y")[m
[32m+[m[32mticker = input("Enter stock ticker (e.g. RELIANCE.NS, TCS.NS, AAPL): ").strip().upper()[m
[32m+[m
[32m+[m[32mprint("\n── Downloading price data...")[m
[32m+[m[32mdata = yf.download(ticker, period="5y", auto_adjust=True)[m
 [m
 if data.empty:[m
[31m-    print("Invalid ticker or no data found.")[m
[32m+[m[32m    print("No data found. Check the ticker and try again.")[m
     exit()[m
 [m
[31m-data["Daily Return"] = data["Close"].pct_change()[m
[31m-[m
[31m-avg_daily_return = data["Daily Return"].mean()[m
[31m-volatility = data["Daily Return"].std()[m
[32m+[m[32mclose = data["Close"].squeeze()[m
 [m
[31m-annual_return = avg_daily_return * 252[m
[31m-annual_volatility = volatility * np.sqrt(252)[m
[32m+[m[32m# ── 1. RETURNS ────────────────────────────────────────────────────────────────[m
[32m+[m[32mdaily_returns = close.pct_change().dropna()[m
[32m+[m[32mannual_return = float(daily_returns.mean() * 252)[m
 [m
[32m+[m[32m# ── 2. RISK ───────────────────────────────────────────────────────────────────[m
[32m+[m[32mannual_volatility = float(daily_returns.std() * np.sqrt(252))[m
 risk_free_rate = 0.02[m
 sharpe_ratio = (annual_return - risk_free_rate) / annual_volatility[m
 [m
[31m-print("\n=== Analysis Report ===\n")[m
[31m-print(f"Ticker: {ticker}")[m
[31m-print(f"Annual Return: {annual_return:.2%}")[m
[31m-print(f"Annual Volatility: {annual_volatility:.2%}")[m
[31m-print(f"Sharpe Ratio: {sharpe_ratio:.2f}")[m
[31m-[m
[31m-plt.figure()[m
[31m-plt.plot(data["Close"])[m
[31m-plt.title(f"{ticker} - Price Chart (5 Years)")[m
[31m-plt.xlabel("Date")[m
[31m-plt.ylabel("Price")[m
[32m+[m[32mif annual_volatility < 0.15:[m
[32m+[m[32m    risk_rating = "LOW"[m
[32m+[m[32m    risk_note = "This stock is relatively stable."[m
[32m+[m[32melif annual_volatility < 0.30:[m
[32m+[m[32m    risk_rating = "MEDIUM"[m
[32m+[m[32m    risk_note = "This stock has moderate swings — manageable for most investors."[m
[32m+[m[32melse:[m
[32m+[m[32m    risk_rating = "HIGH"[m
[32m+[m[32m    risk_note = "This stock swings significantly — be prepared for big moves."[m
[32m+[m
[32m+[m[32m# ── 3. MAX DRAWDOWN ───────────────────────────────────────────────────────────[m
[32m+[m[32mrolling_max = close.cummax()[m
[32m+[m[32mdrawdown = (close - rolling_max) / rolling_max[m
[32m+[m[32mmax_drawdown = float(drawdown.min())[m
[32m+[m[32mmax_dd_date = drawdown.idxmin()[m
[32m+[m[32mif hasattr(max_dd_date, '__iter__') and not isinstance(max_dd_date, str):[m
[32m+[m[32m    max_dd_date = max_dd_date.iloc[0] if len(max_dd_date) > 0 else "N/A"[m
[32m+[m
[32m+[m[32m# ── 4. BENCHMARK COMPARISON ───────────────────────────────────────────────────[m
[32m+[m[32mprint("── Fetching benchmark data...")[m
[32m+[m[32mis_indian = ticker.endswith(".NS") or ticker.endswith(".BO")[m
[32m+[m[32mbench_ticker = "^NSEI" if is_indian else "^GSPC"[m
[32m+[m[32mbench_label = "Nifty 50" if is_indian else "S&P 500"[m
[32m+[m
[32m+[m[32mbenchmark = yf.download(bench_ticker, period="5y", auto_adjust=True)[m
[32m+[m[32mbench_return = None[m
[32m+[m[32moutperformance = None[m
[32m+[m[32mif not benchmark.empty:[m
[32m+[m[32m    bench_daily = benchmark["Close"].squeeze().pct_change().dropna()[m
[32m+[m[32m    bench_return = float(bench_daily.mean() * 252)[m
[32m+[m[32m    outperformance = annual_return - bench_return[m
[32m+[m
[32m+[m[32m# ── 5. OPTIONS SIGNAL ─────────────────────────────────────────────────────────[m
[32m+[m[32mprint("── Fetching options data...")[m
[32m+[m[32moptions_signal = "UNAVAILABLE"[m
[32m+[m[32mtry:[m
[32m+[m[32m    tk = yf.Ticker(ticker)[m
[32m+[m[32m    expirations = tk.options[m
[32m+[m[32m    if expirations:[m
[32m+[m[32m        chain = tk.option_chain(expirations[0])[m
[32m+[m[32m        calls = chain.calls[m
[32m+[m[32m        puts = chain.puts[m
[32m+[m[32m        call_vol = calls["volume"].sum()[m
[32m+[m[32m        put_vol = puts["volume"].sum()[m
[32m+[m[32m        pc_ratio = put_vol / call_vol if call_vol > 0 else None[m
[32m+[m[32m        if pc_ratio:[m
[32m+[m[32m            if pc_ratio < 0.7:[m
[32m+[m[32m                options_signal = f"BULLISH (Put/Call Ratio: {pc_ratio:.2f}) — More calls than puts, market is optimistic"[m
[32m+[m[32m            elif pc_ratio > 1.3:[m
[32m+[m[32m                options_signal = f"BEARISH (Put/Call Ratio: {pc_ratio:.2f}) — More puts than calls, market is cautious"[m
[32m+[m[32m            else:[m
[32m+[m[32m                options_signal = f"NEUTRAL (Put/Call Ratio: {pc_ratio:.2f}) — Balanced market sentiment"[m
[32m+[m[32mexcept:[m
[32m+[m[32m    pass[m
[32m+[m
[32m+[m[32m# ── 6. PRICE TARGET / ANALYST ESTIMATES ───────────────────────────────────────[m
[32m+[m[32mprice_target = None[m
[32m+[m[32mcurrent_price = None[m
[32m+[m[32mupside = None[m
[32m+[m[32mtry:[m
[32m+[m[32m    tk = yf.Ticker(ticker) if 'tk' not in dir() else tk[m
[32m+[m[32m    info = tk.info[m
[32m+[m[32m    current_price = info.get("currentPrice") or info.get("regularMarketPrice")[m
[32m+[m[32m    price_target = info.get("targetMeanPrice")[m
[32m+[m[32m    if current_price and price_target:[m
[32m+[m[32m        upside = ((price_target - current_price) / current_price) * 100[m
[32m+[m[32mexcept:[m
[32m+[m[32m    pass[m
[32m+[m
[32m+[m[32m# ── 7. AI SUMMARY ─────────────────────────────────────────────────────────────[m
[32m+[m[32mprint("── Generating AI summary...")[m
[32m+[m[32mai_summary = "(Add your Anthropic API key above to get AI summaries)"[m
[32m+[m[32mif ANTHROPIC_KEY and ANTHROPIC_KEY != "your-api-key-here":[m
[32m+[m[32m    try:[m
[32m+[m[32m        client = anthropic.Anthropic(api_key=ANTHROPIC_KEY)[m
[32m+[m[32m        prompt = f"""[m
[32m+[m[32m        Analyze this stock data for {ticker} and give a short, plain English smart money summary in 4-5 sentences.[m
[32m+[m[32m        - Annual Return: {annual_return:.2%}[m
[32m+[m[32m        - Annual Volatility: {annual_volatility:.2%}[m
[32m+[m[32m        - Sharpe Ratio: {sharpe_ratio:.2f}[m
[32m+[m[32m        - Max Drawdown: {max_drawdown:.2%}[m
[32m+[m[32m        - Risk Rating: {risk_rating}[m
[32m+[m[32m        - Benchmark ({bench_label}) Return: {bench_return:.2%if bench_return else 'N/A'}[m
[32m+[m[32m        - Options Signal: {options_signal}[m
[32m+[m[32m        - Analyst Price Target: {price_target if price_target else 'N/A'}[m
[32m+[m[32m        - Current Price: {current_price if current_price else 'N/A'}[m
[32m+[m[32m        - Estimated Upside: {f'{upside:.1f}%' if upside else 'N/A'}[m
[32m+[m[32m        Be direct, no fluff. Mention if it's worth considering or avoiding and why.[m
[32m+[m[32m        """[m
[32m+[m[32m        message = client.messages.create([m
[32m+[m[32m            model="claude-opus-4-5",[m
[32m+[m[32m            max_tokens=300,[m
[32m+[m[32m            messages=[{"role": "user", "content": prompt}][m
[32m+[m[32m        )[m
[32m+[m[32m        ai_summary = message.content[0].text[m
[32m+[m[32m    except Exception as e:[m
[32m+[m[32m        ai_summary = f"AI summary failed: {e}"[m
[32m+[m
[32m+[m[32m# ── 8. PRINT REPORT ───────────────────────────────────────────────────────────[m
[32m+[m[32mprint("\n" + "─" * 52)[m
[32m+[m[32mprint(f"  SMART MONEY REPORT: {ticker}")[m
[32m+[m[32mprint("─" * 52)[m
[32m+[m
[32m+[m[32mprint("\n  RETURNS")[m
[32m+[m[32mprint(f"  Annual Return      : {annual_return:+.2%}")[m
[32m+[m[32mif bench_return:[m
[32m+[m[32m    direction = "▲" if outperformance >= 0 else "▼"[m
[32m+[m[32m    print(f"  {bench_label} Return  : {bench_return:+.2%}  ({direction} {abs(outperformance):.2%} {'above' if outperformance >= 0 else 'below'} benchmark)")[m
[32m+[m
[32m+[m[32mprint("\n  RISK")[m
[32m+[m[32mprint(f"  Annual Volatility  : {annual_volatility:.2%}")[m
[32m+[m[32mprint(f"  Sharpe Ratio       : {sharpe_ratio:.2f}  ({'good' if sharpe_ratio > 1 else 'average' if sharpe_ratio > 0 else 'poor'})")[m
[32m+[m[32mprint(f"  Max Drawdown       : {max_drawdown:.2%}  (worst fall, on {max_dd_date})")[m
[32m+[m[32mprint(f"  Risk Rating        : {risk_rating}  — {risk_note}")[m
[32m+[m
[32m+[m[32mprint("\n  ANALYST PRICE TARGET")[m
[32m+[m[32mif current_price and price_target and upside is not None:[m
[32m+[m[32m    direction = "📈" if upside >= 0 else "📉"[m
[32m+[m[32m    print(f"  Current Price      : {current_price:.2f}")[m
[32m+[m[32m    print(f"  Analyst Target     : {price_target:.2f}")[m
[32m+[m[32m    print(f"  Estimated Upside   : {direction} {upside:+.1f}%")[m
[32m+[m[32melse:[m
[32m+[m[32m    print("  Price target data unavailable for this ticker.")[m
[32m+[m
[32m+[m[32mprint("\n  SMART MONEY SIGNAL")[m
[32m+[m[32mprint(f"  Options Signal     : {options_signal}")[m
[32m+[m
[32m+[m[32mprint("\n  AI SUMMARY")[m
[32m+[m[32mprint(f"  {ai_summary}")[m
[32m+[m
[32m+[m[32mprint("\n" + "─" * 52)[m
[32m+[m
[32m+[m[32m# ── 9. CHART ──────────────────────────────────────────────────────────────────[m
[32m+[m[32mfig, (ax1, ax2) = plt.subplots(2, 1, figsize=(12, 8), sharex=True)[m
[32m+[m[32mfig.suptitle(f"{ticker} — Smart Money Analysis", fontsize=14, fontweight="bold")[m
[32m+[m
[32m+[m[32max1.plot(close.index, close, color="#2E86AB", linewidth=1.5, label="Price")[m
[32m+[m[32max1.fill_between(close.index, close, close.cummax(), alpha=0.15, color="#D85A30", label="Drawdown zone")[m
[32m+[m[32max1.set_ylabel("Price")[m
[32m+[m[32max1.legend()[m
[32m+[m[32max1.grid(alpha=0.3)[m
[32m+[m
[32m+[m[32max2.fill_between(drawdown.index, drawdown, 0, color="#D85A30", alpha=0.6, label="Drawdown")[m
[32m+[m[32max2.set_ylabel("Drawdown")[m
[32m+[m[32max2.set_xlabel("Date")[m
[32m+[m[32max2.legend()[m
[32m+[m[32max2.grid(alpha=0.3)[m
[32m+[m
[32m+[m[32mplt.tight_layout()[m
 plt.show()[m
\ No newline at end of file[m
